<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Art of Connection: Se-woong & Se-jeong</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@200;400;700&family=Noto+Serif+KR:wght@200;400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #1a1a1a;
            --accent: #0047FF;
        }
        body {
            font-family: 'Noto Serif KR', serif;
            background-color: #fff;
            color: var(--primary);
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }
        .eng { font-family: 'Montserrat', sans-serif; }
        
        canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: 0.4;
        }

        .section-padding { padding: 120px 24px; }
        
        .reveal { opacity: 0; transform: translateY(30px); }
        
        .image-container {
            overflow: hidden;
            border-radius: 4px;
        }
        .parallax-img {
            transform: scale(1.2);
            transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        /* Copy Feedback */
        #copy-toast {
            position: fixed;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--primary);
            color: #fff;
            padding: 12px 24px;
            border-radius: 50px;
            font-size: 14px;
            z-index: 100;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s;
        }
    </style>
</head>
<body>

    <canvas id="neural-bg"></canvas>

    <!-- Loading Screen -->
    <div id="loader" class="fixed inset-0 z-[100] bg-white flex items-center justify-center">
        <div class="text-center">
            <div class="eng tracking-[0.5em] text-sm mb-4 animate-pulse">OPTIMIZING CONNECTION</div>
            <div class="w-48 h-[1px] bg-gray-100 overflow-hidden">
                <div id="loader-bar" class="h-full bg-black w-0"></div>
            </div>
        </div>
    </div>

    <!-- Hero Section -->
    <section class="h-screen flex flex-col items-center justify-center relative px-6 text-center">
        <div class="eng text-xs tracking-[0.4em] mb-8 reveal uppercase">Wedding Exhibition</div>
        <h1 class="text-5xl md:text-7xl font-light mb-12 tracking-tight leading-tight reveal">
            황세웅 <span class="text-gray-300 font-extralight">&</span> 허세정
        </h1>
        <div class="eng text-lg tracking-widest reveal font-light">
            2026. 06. 27. SAT. 2:00 PM<br>
            MONACO SPACE
        </div>
        <div class="absolute bottom-12 left-50 animate-bounce opacity-30">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><path d="M7 13l5 5 5-5M7 6l5 5 5-5"/></svg>
        </div>
    </section>

    <!-- Quote Section -->
    <section class="section-padding flex flex-col items-center justify-center text-center bg-white/60 backdrop-blur-sm">
        <p class="text-lg md:text-xl leading-relaxed max-w-lg font-light reveal italic">
            "각기 다른 데이터와 알고리즘으로<br>
            자신만의 세계를 그려오던 두 사람이 만났습니다.<br><br>
            이제 하나의 캔버스 위에<br>
            서로의 색을 입혀 가장 완벽한 합을 완성하려 합니다."
        </p>
    </section>

    <!-- Gallery Section -->
    <section class="section-padding max-w-screen-lg mx-auto overflow-hidden">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
            <div class="image-container reveal" style="transition-delay: 0.1s">
                <img src="https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=1000&q=80" alt="Main Photo" class="parallax-img w-full h-[600px] object-cover">
            </div>
            <div class="space-y-12">
                <div class="reveal" style="transition-delay: 0.3s">
                    <h2 class="eng text-3xl font-light mb-4">The Curator</h2>
                    <p class="text-gray-500 leading-relaxed font-light">
                        AI 소프트웨어 연구와 교육에 매진해온 황세웅,<br>
                        데이터 속에서 숨겨진 의미를 찾아내는 허세정.<br>
                        서로의 삶을 가장 아름다운 데이터로 기록해 나갈<br>
                        그 첫 번째 전시회에 당신을 초대합니다.
                    </p>
                </div>
                <div class="grid grid-cols-2 gap-4 reveal" style="transition-delay: 0.5s">
                    <div class="image-container h-48">
                        <img src="https://images.unsplash.com/photo-1511285560929-80b456fea0bc?auto=format&fit=crop&w=600&q=80" alt="Sub Photo 1" class="w-full h-full object-cover">
                    </div>
                    <div class="image-container h-48 mt-8">
                        <img src="https://images.unsplash.com/photo-1520854221256-17451cc331bf?auto=format&fit=crop&w=600&q=80" alt="Sub Photo 2" class="w-full h-full object-cover">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Invitation Info -->
    <section class="section-padding bg-stone-50">
        <div class="max-w-screen-sm mx-auto">
            <div class="mb-20 text-center reveal">
                <h3 class="eng text-xs tracking-[0.4em] mb-10 text-gray-400">FAMILY</h3>
                <div class="text-xl space-y-4 font-light">
                    <p>황종범 · 김서경 <span class="text-sm text-gray-400 mx-2">의 차남</span> <strong class="font-normal">세웅</strong></p>
                    <p>허재구 · 홍수옥 <span class="text-sm text-gray-400 mx-2">의 장녀</span> <strong class="font-normal">세정</strong></p>
                </div>
            </div>

            <div class="mb-20 text-center reveal">
                <h3 class="eng text-xs tracking-[0.4em] mb-10 text-gray-400">VENUE</h3>
                <p class="text-xl mb-2 font-light">모나코 스페이스</p>
                <p class="text-gray-500 text-sm mb-8 font-light leading-relaxed">
                    서울 서초구 서초대로 397 부띠크모나코 B1<br>
                    (강남역 9번 출구에서 200m)
                </p>
                <a href="https://map.kakao.com/?q=모나코스페이스" target="_blank" class="inline-block border border-black px-8 py-3 text-sm eng tracking-widest hover:bg-black hover:text-white transition-all">
                    OPEN MAP
                </a>
            </div>

            <div class="text-center reveal">
                <h3 class="eng text-xs tracking-[0.4em] mb-10 text-gray-400">GIFTS</h3>
                <div class="bg-white p-8 border border-gray-100 shadow-sm inline-block w-full">
                    <p class="text-sm text-gray-500 mb-4">신랑 측 마음 전하실 곳</p>
                    <div class="flex items-center justify-between gap-4 border-b border-gray-50 pb-4 mb-4">
                        <span class="text-base font-light">토스뱅크 1000-3101-7029 황세웅</span>
                        <button onclick="copyText('1000-3101-7029')" class="text-xs text-blue-600 eng font-bold">COPY</button>
                    </div>
                    <p class="text-[10px] text-gray-400">보내주신 축복은 저희의 소중한 시작에 밑거름으로 사용하겠습니다.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="section-padding text-center bg-white">
        <div class="eng text-[10px] tracking-[0.5em] text-gray-300">
            OPTIMIZED & CURATED BY GEMINI & SEWOONG
        </div>
    </footer>

    <div id="copy-toast">계좌번호가 복사되었습니다.</div>

    <script>
        // 1. Loading Animation
        window.addEventListener('load', () => {
            gsap.to("#loader-bar", {
                width: "100%",
                duration: 1.5,
                ease: "power2.inOut",
                onComplete: () => {
                    gsap.to("#loader", {
                        opacity: 0,
                        duration: 1,
                        onComplete: () => {
                            document.getElementById('loader').style.display = 'none';
                            initAnimations();
                        }
                    });
                }
            });
        });

        // 2. Neural Background (Canvas)
        const canvas = document.getElementById('neural-bg');
        const ctx = canvas.getContext('2d');
        let dots = [];
        const dotCount = 60;
        const maxDistance = 150;

        function initCanvas() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            dots = [];
            for(let i=0; i<dotCount; i++) {
                dots.push({
                    x: Math.random() * canvas.width,
                    y: Math.random() * canvas.height,
                    vx: (Math.random() - 0.5) * 0.5,
                    vy: (Math.random() - 0.5) * 0.5
                });
            }
        }

        function drawCanvas() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            dots.forEach((dot, i) => {
                dot.x += dot.vx;
                dot.y += dot.vy;
                if(dot.x < 0 || dot.x > canvas.width) dot.vx *= -1;
                if(dot.y < 0 || dot.y > canvas.height) dot.vy *= -1;

                ctx.beginPath();
                ctx.arc(dot.x, dot.y, 1.5, 0, Math.PI * 2);
                ctx.fillStyle = '#0047FF';
                ctx.fill();

                for(let j=i+1; j<dotCount; j++) {
                    const dot2 = dots[j];
                    const dist = Math.sqrt((dot.x - dot2.x)**2 + (dot.y - dot2.y)**2);
                    if(dist < maxDistance) {
                        ctx.beginPath();
                        ctx.moveTo(dot.x, dot.y);
                        ctx.lineTo(dot2.x, dot2.y);
                        ctx.strokeStyle = `rgba(0, 71, 255, ${1 - dist/maxDistance})`;
                        ctx.lineWidth = 0.5;
                        ctx.stroke();
                    }
                }
            });
            requestAnimationFrame(drawCanvas);
        }

        window.addEventListener('resize', initCanvas);
        initCanvas();
        drawCanvas();

        // 3. GSAP Animations
        gsap.registerPlugin(ScrollTrigger);

        function initAnimations() {
            // Reveal animations
            gsap.utils.toArray('.reveal').forEach((elem) => {
                gsap.to(elem, {
                    scrollTrigger: {
                        trigger: elem,
                        start: "top 85%",
                        toggleActions: "play none none none"
                    },
                    opacity: 1,
                    y: 0,
                    duration: 1.2,
                    ease: "power3.out"
                });
            });

            // Parallax Images
            gsap.utils.toArray('.parallax-img').forEach((img) => {
                gsap.to(img, {
                    scrollTrigger: {
                        trigger: img,
                        start: "top bottom",
                        end: "bottom top",
                        scrub: true
                    },
                    scale: 1,
                    y: -30,
                    ease: "none"
                });
            });
        }

        // 4. Utility Functions
        function copyText(text) {
            const tempInput = document.createElement("input");
            tempInput.value = text;
            document.body.appendChild(tempInput);
            tempInput.select();
            document.execCommand("copy");
            document.body.removeChild(tempInput);

            const toast = document.getElementById('copy-toast');
            toast.style.opacity = '1';
            setTimeout(() => {
                toast.style.opacity = '0';
            }, 2000);
        }
    </script>
</body>
</html>